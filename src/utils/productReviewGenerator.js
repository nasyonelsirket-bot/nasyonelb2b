/**
 * Ürüne özel değerlendirme metinleri (tarayıcı — lib/productReviewGenerator.cjs ile aynı mantık)
 */
export { REVIEWS_VERSION, generateProductReviews, detectProductKind, shortName } from '../../lib/productReviewGenerator.cjs';
